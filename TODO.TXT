Service:
    register function
        creates folder for user
        writes info to user.txt

        bug with reading in string (ask), due to static buffer#
            fix: cpy string after ask
    login function
        checks user.txt and validate / cancel connection
    (read all data from file in vuln buffer)

    about me
        prints own user info

    upload wav  -> temp.wav
    upload pcm  -> temp.pcm
    synth pcm
    synth wav
        same as pcm

    docker integration